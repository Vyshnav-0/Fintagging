# Example backend .env (do NOT commit real keys to git)
# Copy this to `.env` and fill with real values for local development.

PORT=5000
NODE_ENV=development
# Put your real Gemini API key here (or leave blank to force local extraction)
GEMINI_API_KEY=
# Put your HuggingFace API key here if used
HUGGINGFACE_API_KEY=

# Dataset configurations (example values)
FINNI_DATASET_ID=TheFinAI/FinNI-eval
FINCL_DATASET_ID=TheFinAI/FinCL-eval

# Use memory storage for development without MongoDB
USE_MEMORY_STORAGE=true

# Toggle to force local rule-based extraction (true|false)
# When true, the app will use the local regex-based numeric extractor and will NOT call the Gemini API.
USE_LOCAL_EXTRACTION=false

# Uncomment and set the following when MongoDB is needed
# MONGODB_URI=mongodb://localhost:27017/fintagging
